$(document).ready;


$(function() $("#submitBtn").on("click", function(event));
        var formFields = ($(".blank"));
        var formError = ($(".errorMsg")); console.log(formFields);

        event.preventDefault();

        for (var i = 0; i < formFields.length; i++) {
            if (!formFields[i].value) {
                $(formFields[i].addClass("error"); $(formError[i].show();
                    }

                });
            $(".blank").on("keydown", function() {
                        var (forFields) = ($ ".blank");

                        var formError = ($(“.error - message”));
                        for (var i = 0; i < formFields.length; i++) {
                            if (!formFields[i].value) {
                                $(formFields[i].addClass("error"); $(formError[i].show();
                                    }
                                }
                            });
